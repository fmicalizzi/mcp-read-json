{
  "name": "mcp-read-json",
  "version": "1.0.0",
  "description": "MCP server for reading JSON knowledge base without modifications",
  "license": "MIT",
  "type": "module",
  "bin": {
    "mcp-read-json": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && chmod +x dist/*.js",
    "start": "node dist/index.js",
    "dev": "tsc --watch",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.0.1"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5.6.2"
  }
}
